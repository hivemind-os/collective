{
  "name": "@agentic-mesh/e2e-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src vitest.config.ts",
    "test": "vitest",
    "clean": "rimraf dist .vitest"
  },
  "dependencies": {
    "@agentic-mesh/core": "workspace:*",
    "@agentic-mesh/daemon": "workspace:*",
    "@agentic-mesh/types": "workspace:*",
    "@mysten/sui": "^1.30.0"
  },
  "devDependencies": {
    "fastify": "^5.8.5",
    "vitest": "^3.0.9"
  }
}
