{
  "name": "@hivemind-os/collective-example-two-agent-task",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "prestart": "pnpm --dir ..\\.. --filter @hivemind-os/collective-types run build && pnpm --dir ..\\.. --filter @hivemind-os/collective-core run build",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@hivemind-os/collective-core": "workspace:*",
    "@hivemind-os/collective-types": "workspace:*",
    "@mysten/sui": "^1.30.0",
    "tsx": "^4.0.0"
  }
}
